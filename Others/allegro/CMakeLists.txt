CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(allegro)

SET(DEPDIR "${CMAKE_CURRENT_SOURCE_DIR}")
#SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wl,-Bsymbolic-functions")

INCLUDE_DIRECTORIES(${DEPDIR})
LINK_DIRECTORIES(${DEPDIR})

ADD_EXECUTABLE(allegro adapter.cc)
TARGET_LINK_LIBRARIES(allegro alleg)
